name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: build
    runs-on: ubuntu-24.04
    timeout-minutes: 25

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          cache-provider: basic

      - name: Verify distribution contract
        run: python3 scripts/verify-distribution.py

      - name: Verify release tooling syntax
        run: |
          python3 -m py_compile scripts/resolve-release-version.py scripts/create-release-manifest.py
          test "$(python3 scripts/resolve-release-version.py v1.2.3)" =         run: ./gradlew build lint --stacktrace
version_name=1.2.3\nversion_code=1002003'

      - name: Build and lint
        run: ./gradlew build lint --stacktrace
