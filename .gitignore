*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/.kotlin

# Model weights ARE tracked in this official distribution. Sailens Android deliberately ignores
# the same paths because the reusable platform ships zero weights. The current bundled weights are
# part of why this distribution is released under AGPL-3.0. Do not add an ignore rule for
# app/src/main/assets/*.tflite.
